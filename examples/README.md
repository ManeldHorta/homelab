# Examples

This directory contains reusable examples and templates related to the homelab.

Examples may include:

- Docker Compose templates;
- environment-variable templates;
- configuration examples;
- operational command examples;
- monitoring examples;
- service integration examples.

Examples must never contain real passwords, API tokens, authentication keys, or other secrets.

Use placeholder values for sensitive configuration.
